Index: src/main/resources/templates/transactions/form.html
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/resources/templates/transactions/form.html b/src/main/resources/templates/transactions/form.html
--- a/src/main/resources/templates/transactions/form.html
+++ b/src/main/resources/templates/transactions/form.html	(date 1753364409680)
@@ -6,22 +6,22 @@
     <title th:text="${transaction.id == null ? 'Thêm Giao dịch Mới' : 'Sửa Giao dịch'}"></title>
     <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
-    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/material_blue.css"> <style>
-    body { padding-top: 56px; }
-    .footer {
-        position: relative;
-        bottom: 0;
-        width: 100%;
-        height: 60px;
-        line-height: 60px;
-        background-color: #f5f5f5;
-        margin-top: 50px;
-    }
-    /* Tùy chỉnh nhỏ cho Flatpickr nếu cần (ví dụ để căn chỉnh icon nếu sử dụng) */
-    .flatpickr-calendar {
-        z-index: 1050; /* Đảm bảo lịch hiển thị trên các phần tử khác của Bootstrap */
-    }
-</style>
+    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/material_blue.css">
+    <style>
+        body { padding-top: 56px; }
+        .footer {
+            position: relative;
+            bottom: 0;
+            width: 100%;
+            height: 60px;
+            line-height: 60px;
+            background-color: #f5f5f5;
+            margin-top: 50px;
+        }
+        .flatpickr-calendar {
+            z-index: 1050;
+        }
+    </style>
 </head>
 <body>
 
@@ -57,10 +57,15 @@
 
         <div class="form-group">
             <label for="category">Danh mục:</label>
-            <select id="category" th:field="*{category.id}" class="form-control" required>
-                <option value="">Chọn danh mục</option>
-                <option th:each="cat : ${categories}" th:value="${cat.id}" th:text="${cat.name}"></option>
-            </select>
+            <div class="input-group">
+                <select id="category" th:field="*{category.id}" class="form-control" required>
+                    <option value="">Chọn danh mục</option>
+                    <option th:each="cat : ${categories}" th:value="${cat.id}" th:text="${cat.name}"></option>
+                </select>
+                <div class="input-group-append">
+                    <a href="/categories/new" class="btn btn-primary">Thêm danh mục</a>
+                </div>
+            </div>
             <div th:if="${#fields.hasErrors('category.id')}" th:errors="*{category.id}" class="text-danger"></div>
         </div>
 
@@ -92,19 +97,19 @@
 <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8aSjF4sV1wA6jWJ" crossorigin="anonymous"></script>
 
 <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
-<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/vn.js"></script> <script>
+<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/vn.js"></script>
+<script>
     $(document).ready(function() {
         flatpickr("#transactionDate", {
-            enableTime: true,           // Kích hoạt chọn thời gian
-            noCalendar: false,          // Đảm bảo lịch hiển thị
-            dateFormat: "Y-m-d H:i",    // Định dạng gửi về server (phù hợp với LocalDateTime)
-            altInput: true,             // Sử dụng input thứ cấp để hiển thị định dạng thân thiện hơn
-            altFormat: "d/m/Y H:i",     // Định dạng hiển thị cho người dùng (ví dụ: 16/07/2025 09:54)
-            time_24hr: true,            // Sử dụng định dạng 24 giờ
-            locale: "vn",               // Sử dụng ngôn ngữ tiếng Việt (nếu đã nhúng gói vn.js)
-            allowInput: true,           // Cho phép người dùng gõ trực tiếp
-            // clickOpens: true,        // Mặc định là true, không cần thiết phải đặt rõ ràng
-            wrap: false                 // Không bọc input trong một div cha nếu không muốn thêm icon bên ngoài
+            enableTime: true,
+            noCalendar: false,
+            dateFormat: "Y-m-d H:i",
+            altInput: true,
+            altFormat: "d/m/Y H:i",
+            time_24hr: true,
+            locale: "vn",
+            allowInput: true,
+            wrap: false
         });
     });
 </script>
